{
  "name": "fiori-sample-app",
  "version": "1.0.0",
  "private": true,
  "description": "SAP Fiori Sample App",
  "scripts": {
    "start": "ui5 serve -o index.html",
    "build": "ui5 build --clean-dest --all"
  },
  "devDependencies": {
    "@ui5/cli": "^3.0.0"
  }
}
