{
  "name": "fiori-sample-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "ui5 build --clean-dest",
    "start": "node server.js"
  },
  "devDependencies": {
    "@ui5/cli": "^3.0.0"
  }
}
